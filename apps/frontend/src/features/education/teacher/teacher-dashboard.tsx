/**
 * TeacherFeatureDashboard — teacher assignment and class insight UI (US4).
 */
export default function TeacherFeatureDashboard() {
  return <div className="text-kidpen-text">Teacher Dashboard — assignments & insights</div>;
}