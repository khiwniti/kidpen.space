/**
 * LessonViewer — displays interactive lesson content with checkpoints (US2).
 *
 * Integrated with useLesson hook for state management and checkpoint submission.
 * Calls interaction writer + mastery update service on each checkpoint.
 */
export default function LessonViewer({ lessonId }: { lessonId: string }) {
  // Stub — full component built when hook + API are integrated
  return <div className="text-kidpen-text">Lesson Viewer for {lessonId}</div>;
}