/**
 * ParentDashboard — guardian progress view + consent controls (US5).
 */
export default function ParentDashboard() {
  return <div className="text-kidpen-text">ผู้ปกครอง — ดูความก้าวหน้าของบุตรหลาน</div>;
}