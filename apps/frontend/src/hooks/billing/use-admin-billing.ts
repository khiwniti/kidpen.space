import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { backendApi } from '@/lib/api-client';
export function useUserBillingSummary(accountId:string|null){return useQuery({queryKey:['admin','billing','summary',accountId],queryFn:async()=>{const r=await backendApi.get(`/admin/billing/user/${accountId}/summary`);if(r.error)throw new Error(r.error.message);return r.data;},enabled:!!accountId,staleTime:30000});}
export function useAdminUserTransactions(p:{accountId:string;page?:number;page_size?:number;type_filter?:string}){return useQuery({queryKey:['admin','billing','transactions',p.accountId],queryFn:async()=>{const r=await backendApi.get(`/admin/billing/user/${p.accountId}/transactions`);if(r.error)throw new Error(r.error.message);return r.data;},enabled:!!p.accountId,staleTime:30000});}
export function useAdjustCredits(){const qc=useQueryClient();return useMutation({mutationFn:async(req:any)=>{const r=await backendApi.post('/admin/billing/credits/adjust',req);if(r.error)throw new Error(r.error.message);return r.data;},onSuccess:(_,v)=>{qc.invalidateQueries({queryKey:['admin','billing','summary',v.account_id]});}});}
export function useProcessRefund(){const qc=useQueryClient();return useMutation({mutationFn:async(req:any)=>{const r=await backendApi.post('/admin/billing/refund',req);if(r.error)throw new Error(r.error.message);return r.data;},onSuccess:(_,v)=>{qc.invalidateQueries({queryKey:['admin','billing','summary',v.account_id]});}});}
