---
wave: 2
autonomous: true
objective: Implement Bayesian Knowledge Tracing (pyBKT) for mastery tracking and difficulty adaptation.
depends_on: [02-03]
---

<objective>
Integrate pyBKT for client-side mastery tracking. Map knowledge components to Thai curriculum hierarchy and connect mastery outputs to tutoring difficulty adaptation.
</objective>

## Task 1: Create pyBKT WASM wrapper
- **read_first**: Research `pyBKT` library for WASM compilation
- **action**: Create `apps/frontend/src/lib/mastery/pybkt-wrapper.ts` with:
  - WASM module loading and initialization
  - TypeScript type definitions for pyBKT API
  - Error handling for WASM failures
  - Memory management helpers
- **acceptance_criteria**:
  - WASM module loads successfully
  - TypeScript types match pyBKT API
  - Handles WASM initialization errors
  - Provides cleanup function for memory

## Task 2: Define knowledge component hierarchy
- **read_first**: Thai curriculum standards (ม.1-ม.6)
- **action**: Create `apps/frontend/src/lib/mastery/knowledge-components.ts` with:
  - Subject → Topic → Concept hierarchy
  - Math: Number sense, Algebra, Geometry, Statistics
  - Science: Physics, Chemistry, Biology, Earth Science
  - Knowledge component IDs and prerequisites
  - Grade-level alignment (ม.1-ม.6)
- **acceptance_criteria**:
  - Hierarchy covers core STEM topics
  - Each KC has unique ID and display name (Thai)
  - Prerequisites are explicitly defined
  - Grade alignment is accurate

## Task 3: Implement mastery state manager
- **read_first**: Tasks 1-2 outputs
- **action**: Create `apps/frontend/src/lib/mastery/mastery-manager.ts` with:
  - Initialize BKT models for each knowledge component
  - Update mastery on correct/incorrect responses
  - Track guess and slip parameters per KC
  - Calculate overall subject mastery
- **acceptance_criteria**:
  - Updates mastery probability on each response
  - Handles multiple subjects simultaneously
  - Persists state to IndexedDB
  - Provides mastery summary by subject/topic

## Task 4: Create IndexedDB schema for mastery
- **read_first**: `apps/frontend/src/lib/mastery/`
- **action**: Create `apps/frontend/src/lib/mastery/storage.ts` with:
  - Dexie.js schema for mastery state
  - CRUD operations for mastery records
  - Migration support for schema changes
  - Export/import for Drive sync
- **acceptance_criteria**:
  - Schema stores per-KC mastery data
  - Supports efficient queries by subject
  - Handles schema migrations
  - Provides data export format

## Task 5: Build difficulty adaptation module
- **read_first**: `apps/frontend/src/lib/mastery/mastery-manager.ts`
- **action**: Create `apps/frontend/src/lib/mastery/difficulty-adapter.ts` with:
  - Map mastery probability to difficulty level
  - Adaptive problem selection algorithm
  - Zone of proximal development targeting
  - Mastery threshold for advancement
- **acceptance_criteria**:
  - Low mastery → easier problems
  - High mastery → harder problems
  - Targets 70-80% success probability
  - Prevents frustration (too hard) and boredom (too easy)

## Task 6: Create React hooks for mastery
- **read_first**: All previous task outputs
- **action**: Create `apps/frontend/src/hooks/useMastery.ts` with:
  - Get mastery for current subject/topic
  - Update mastery on response
  - Get recommended difficulty
  - Subscribe to mastery changes
- **acceptance_criteria**:
  - Hook returns `{ mastery, updateMastery, recommendedDifficulty }`
  - Works with React 18+ strict mode
  - Persists updates automatically
  - Efficient re-renders

## Task 7: Add unit tests for mastery tracking
- **read_first**: All previous task outputs
- **action**: Create `apps/frontend/src/lib/mastery/__tests__/` with:
  - BKT probability update tests
  - Knowledge component hierarchy tests
  - Difficulty adaptation tests
  - Storage persistence tests
- **acceptance_criteria**:
  - Tests verify mastery updates correctly
  - Tests verify difficulty adaptation logic
  - Tests verify storage operations
  - All tests pass

---

## Verification Steps

1. Run `pnpm test apps/frontend/src/lib/mastery/__tests__/`
2. Manual test: Answer questions, verify mastery updates
3. Verify difficulty adapts to performance
4. Check IndexedDB for persisted mastery data
5. Verify mastery survives page reload

## Must-Haves

- [ ] Mastery probability updates on each response
- [ ] Difficulty adapts based on mastery level
- [ ] Knowledge components map to Thai curriculum
- [ ] Mastery persists across sessions
- [ ] Integration with tutoring session

## Files Modified

- `apps/frontend/src/lib/mastery/pybkt-wrapper.ts` (new)
- `apps/frontend/src/lib/mastery/knowledge-components.ts` (new)
- `apps/frontend/src/lib/mastery/mastery-manager.ts` (new)
- `apps/frontend/src/lib/mastery/storage.ts` (new)
- `apps/frontend/src/lib/mastery/difficulty-adapter.ts` (new)
- `apps/frontend/src/hooks/useMastery.ts` (new)
- `apps/frontend/src/lib/mastery/__tests__/*.test.ts` (new)