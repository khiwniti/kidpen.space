---
wave: 1
depends_on: []
autonomous: true
gap_closure: true
objective: Close authentication and localization gaps by implementing and validating Google OAuth callback flow with Thai-ready sign-in behavior.
requirements: []
files_modified:
  - apps/frontend/src/components/GoogleSignIn.tsx
  - apps/frontend/src/app/auth/callback/page.tsx
  - apps/frontend/translations/th.json
---

<objective>
Implement missing authentication completion pieces for Phase 1 so Google OAuth can complete end-to-end and expose Thai user-facing sign-in behavior.
</objective>

<task id="1" type="auto">
  <description>Implement auth callback page route handling successful and failed OAuth redirects</description>
  <read_first>
    <file>apps/frontend/src/components/GoogleSignIn.tsx</file>
    <file>apps/frontend/src/app/auth/page.tsx</file>
  </read_first>
  <action>
    Create `apps/frontend/src/app/auth/callback/page.tsx` that reads auth query params, renders success/error states, and redirects authenticated users to the dashboard route.
    Include explicit handling for missing/invalid callback parameters and render a recoverable error UI with a retry button back to `/auth`.
  </action>
  <acceptance_criteria>
    <criterion>`apps/frontend/src/app/auth/callback/page.tsx` exists</criterion>
    <criterion>File contains exported default React component</criterion>
    <criterion>File handles both success and error query param paths</criterion>
    <criterion>File includes redirect path to authenticated area</criterion>
  </acceptance_criteria>
</task>

<task id="2" type="auto">
  <description>Update Google sign-in redirect construction for explicit callback correctness</description>
  <read_first>
    <file>apps/frontend/src/components/GoogleSignIn.tsx</file>
    <file>apps/frontend/src/app/auth/callback/page.tsx</file>
  </read_first>
  <action>
    Update `GoogleSignIn.tsx` so redirect target is consistently `${window.location.origin}/auth/callback` and preserves optional return URL safely.
    Ensure malformed return URL input falls back to root and does not break callback completion.
  </action>
  <acceptance_criteria>
    <criterion>`GoogleSignIn.tsx` contains `/auth/callback` redirect string</criterion>
    <criterion>Return URL handling includes a fallback path</criterion>
    <criterion>Component compiles with existing imports</criterion>
  </acceptance_criteria>
</task>

<task id="3" type="auto">
  <description>Add Thai auth-facing copy for callback and retry states</description>
  <read_first>
    <file>apps/frontend/translations/th.json</file>
    <file>apps/frontend/src/app/auth/callback/page.tsx</file>
  </read_first>
  <action>
    Add Thai translation keys for callback loading/success/error/retry in `apps/frontend/translations/th.json`.
    Wire callback page text to these keys (fallback to existing text if localization utility is unavailable in this route).
  </action>
  <acceptance_criteria>
    <criterion>`th.json` contains auth callback keys for loading, success, error, retry</criterion>
    <criterion>Callback page references Thai translation keys or Thai fallback strings</criterion>
  </acceptance_criteria>
</task>

<verification>
- Run frontend typecheck/build command if available.
- Confirm callback route file exists and imports resolve.
</verification>

<must_haves>
- OAuth callback route exists and handles success/failure states.
- Sign-in flow points to callback consistently.
- Thai auth-facing callback copy exists.
</must_haves>
